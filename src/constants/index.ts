export enum ROLES {
  CUSTOMER = 'customer',
  ADMIN = 'admin',
  MANAGER = 'manager',
}

export enum PriceTypeEnum {
  base = 'base',
  additional = 'additional',
}

export enum MessageBrokerTopics {
  product = 'product',
}
